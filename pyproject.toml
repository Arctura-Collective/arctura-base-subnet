[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "arctura-base-subnet"
version = "0.1.0"
description = "The first open-source Bittensor subnet bridging Base blockchain intelligence into the decentralized AI network."
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
    { name = "Arctura Collective", email = "council@arctura.network" }
]
keywords = [
    "bittensor", "base", "subnet", "blockchain", "decentralized-ai",
    "resonance-bft", "attestation", "agentkit", "merkle"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
requires-python = ">=3.10"

dependencies = [
    "bittensor>=10.5.0,<11.0.0",
    "web3>=6.0.0,<8.0.0",
    "pydantic>=2.0.0,<3.0.0",
    "python-dotenv>=1.0.0,<2.0.0",
]

[project.optional-dependencies]
agentkit = [
    "coinbase-agentkit>=0.1.0",
    "cdp-sdk>=0.0.1",
]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "mypy>=1.5.0",
    "black>=23.9.0",
    "ruff>=0.1.0",
    "bandit>=1.7.5",
    "types-requests>=2.31.0",
]

[project.urls]
Homepage = "https://arctura.network/base/"
Repository = "https://github.com/Arctura-Collective/arctura-base-subnet"
Documentation = "https://arctura.network/base/"
"Bug Tracker" = "https://github.com/Arctura-Collective/arctura-base-subnet/issues"

[project.scripts]
arctura = "arctura_base.cli:main"
arctura-benchmark = "arctura_base.benchmark:main"
arctura-evidence = "arctura_base.evidence:main"
arctura-collect-evidence = "arctura_base.evidence_collect:main"
arctura-cloudwatch-metrics = "arctura_base.cloudwatch_metrics:main"
arctura-coverage-gate = "arctura_base.coverage_gate:main"
arctura-mainnet-approval = "arctura_base.mainnet_approval:main"
arctura-validator-failover-plan = "arctura_base.failover:main"
arctura-treasury-plan = "arctura_base.treasury:main"
arctura-miner = "neurons.miner:main"
arctura-validator = "neurons.validator:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["arctura_base*", "neurons*"]

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]

[tool.ruff]
line-length = 100
extend-exclude = ["gordon.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
strict = false
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
follow_imports = "skip"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["arctura_base", "neurons"]
omit = ["tests/*"]
